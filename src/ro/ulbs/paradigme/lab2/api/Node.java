package ro.ulbs.paradigme.lab2.api;

// Interfață pentru un nod generic
public interface Node {
    // Metodă pentru a returna valoarea nodului
    int getValue();

    // Metodă pentru a seta valoarea nodului
    void setValue(int value);
}